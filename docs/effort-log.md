# Effort Log — career-agent

Time analysis based on git history (commit timestamps).

## Methodology

Time estimated by **commit clusters**, not calendar day. Handles multiple sessions
per day with reasonable accuracy.

**Rules:**

1. **Cluster** = sequence of commits with gaps ≤ 1 hour between consecutive commits.
   Gap > 1 hour ends the cluster and starts a new one.
2. **Session time** = timestamp of last commit in cluster − timestamp of first commit.
3. **Single-commit cluster** → estimated at **~15 min** (minimum).
4. Multiple clusters in one day → multiple rows.
5. Time before first commit (planning, discussion) and after last commit (testing)
   is NOT counted — the number is a lower bound on real time.
6. Commits across related repos (knowledge-mirror-parser, callback-cv) included
   when they fall in the same cluster window.

**Why this works:** cluster-based estimation reflects actual work cadence without
manual tracking. Git history is the source of truth.

---

## Session log

| Date | Commits | Work description | Session time |
|------|---------|-----------------|-------------|
| 28.05.2026 | 1 (career-agent) | **Architecture design session**: project vision, technology stack decisions (PydanticAI, aiogram 3.x, httpx, aiosqlite), adapter pattern, contract-first design, HTTP-first service separation, async safety analysis, EPIC-1 HTML inspection (Djinni/DOU CSS selectors via curl). Init commit: BACKLOG.md, Q&A notes. ⚠️ Highly understated — most work was pre-commit design discussion. | ~15 min (git lower bound) |
| 29.05.2026 | 22 (career-agent) + 1 (kmp) | **EPIC-1**: ARCHITECTURE.md, delivery docs, docker-compose, kmp FastAPI endpoint, `contracts/parsed_document.py`, `adapters/kmp_adapter.py`. **EPIC-2**: `db/schema.sql` + `db/database.py` (7 CRUD helpers), 10 tests. **EPIC-3**: `core/llm_client.py` (ClaudeProvider cache_control=ephemeral, OllamaProvider stub), 12 tests. **EPIC-4**: `core/telegram.py` (aiogram 3.x, chat_id guard, inline keyboards, split helper). **EPIC-5**: `core/tool_registry.py`, `core/router.py` (PydanticAI Agent), `core/settings.py`, `agent.py` entry point. **EPIC-6**: 5 prompt files extracted from SKILL.md (`phase1–4.md`). **EPIC-7**: `tools/cv_fetch_jd.py` + `core/deps.py` (AgentDeps DI pattern), 15 tests. **EPIC-8**: `tools/cv_analyze.py` (Phase 1+2, Quick Scan extraction), 15 tests. **EPIC-9**: `tools/cv_generate.py` (Phase 3+3.5, SUMMARY split), `adapters/cv_adapter.py` (subprocess→pdf), 20 tests. Effort log created. 100/100 tests. ⚠️ Spanned 2 context windows; effort log written mid-session. | ~2 h 31 min (17:29–20:00) |
| 29.05.2026 *(2nd cluster)* | 6 (career-agent) | **EPIC-10**: `tools/cv_cover.py` (Phase 4, Cover.md → Telegram). **EPIC-11**: `tools/cv_get_tracker.py` (SQLite → Telegram summary, fit score regex). **Logging**: `time.monotonic()` timing on all LLM + HTTP calls; DB state machine transition logs. **RSS Watcher**: `core/rss_watcher.py` (asyncio.Task, polls seen_jobs.json, seeds from DB on start), `scripts/emit_vacancy.py`. **import_tracker**: `scripts/import_tracker.py` reads callback-cv/tracker.json → 46 vacancies into SQLite (1 true dupe skipped). **EPIC-12 Web tracker**: `web/reader.py` (VacancyView + regex parsers), `web/api.py` (FastAPI standalone, markdown Jinja2 filter), `web/templates/tracker.html` (CSS+JS from callback-cv verbatim, Jinja2 loop). BACKLOG.md cleaned up — done items moved to ✅ section. | ~44 min (20:59–21:43) |
| 30.05.2026 | — (no commits during session) | **Token economics & quality tuning**: Extended Thinking enabled (budget=10k) for phase1+2 in `ClaudeProvider.complete()` with auto max_tokens raise; model switched to `claude-sonnet-4-6`; SDK 0.105.2 — `betas` param removed (caching now GA). DB schema expanded: `profile_tokens`, `prompt_tokens`, `user_tokens`, `budget_tokens`, `thinking_tokens`, `elapsed_ms` in `llm_usage`. Prompt overhaul: language rule repositioned directly before Output Format; analytical tone rule added; Quick Scan code fence removed (caused empty output). Test runs v3 (Quick Scan bug found) → v4 (fixed, Russian output confirmed). Billing analysis: first CSV had lag, formula `_calc_cost()` confirmed correct — thinking tokens billed as output at $15/MTok. `docs/discovery/Tokenomics.md` fully updated; old `.claude/memory/token-tracking.md` deleted. | ~4 h (estimated) |
| 31.05.2026 | 1 (career-agent) + 1 (callback-cv) | **Phase 2 redesign & pipeline evolution**: Analyzed v4 quality vs original callback-cv output (external review file). Phase 2 prompt full rewrite: 3-way Verdict, Key Barriers/Hidden Risks/Warnings separation, explicit scoring guidance (baseline 5.0, numeric deltas), mandatory Fit Breakdown ✅/⚠️/❌, conditional Adaptation Plan, Internal Analysis section. Phase 1/3/3.5 minor updates (archetype-aware). PROFILE.md `Archetype & Role Positioning` section added. ARCHITECTURE.md `User Profile Schema`. BACKLOG.md: onboarding fields, Pipeline Cost Preview P1 feature. v5 broken (prompt `###` vs `##` header conflict), v6 fixed and working — score 5.5/10, full 4-section Phase 2. Billing verified: v5+v6=$0.22, formula ✅. `Pipeline-Evolution.md` created. | ~3 h 30 min (estimated) |
| 31.05.2026 *(2nd cluster)* | 10 (career-agent) | **Product pivot & docs**: README full rewrite (manifesto, ICP PdM/PO/PM, counselor framing, product decisions). ARCHITECTURE.md ADR: vertical-product, monorepo consolidation, callback-cv dissolution, onboarding PDF→interview→profile, multi-user design. BACKLOG P0: 3 red epics. .gitignore hardened (inline comment bug, CLAUDE.md + docs/discovery/ untracked). Bug fix: both system blocks cached (test + docstring corrected, 12/12 pass). Product naming: Career Agent, "For your next career move". | ~3 h (estimated) |
| 31.05.2026 *(3rd cluster)* | 2 (career-agent) | **Repositioning & dev planning**: README full realignment to Career Agent identity (removed agent roster, fixed platform framing, updated architecture diagram to target state). ARCHITECTURE.md: strategic ADRs — MCP/API for AI agent interoperability, channel-agnostic UI strategy (PWA + WhatsApp roadmap). Product thesis doc (Russian): market rationale, counter-cyclical thesis, monetization models, both-sides pain analysis. Service audits: callback-cv, knowledge-mirror-parser, job-board-monitor — keep/cut/complexity per service. 6-phase development plan. Repositioning Pivot Plan document created. | ~2 h (estimated) |
| 01.06.2026 | — (no commits) | **PDF rendering & multi-skill architecture**: Quick Scan two-column layout (30/70, left-align both), Fit score dot-bar restored with colored ●/○ bar, Verdict banner non-aggressive. Font switched Calibri → Segoe UI, `CAREER_AGENT_FONTS` env var added. `skill/SKILL_TYPES.md` created (PM vs Generic reference table). `prompts/` restructured — all 5 phases split into `prompts/pm/` and `prompts/generic/`. `PROFILE.md` users/001: `skill_type: pm`, users/002: `skill_type: generic`. `SKILL.md` routing updated (all phases via `prompts/[skill_type]/`). AlphaNova JD analysis regenerated with generic prompts (Role Type vs Archetype, generic Fit Dimensions). | ~2.5 h (estimated) |
| 01.06.2026 *(2nd session)* | — (no commits) | **SOLAR Digital — full pipeline run (Alex / 001)**: EN + UA CV generated and iterated (6+ Summary rounds). CV summary: B2B platform/operational system/early-stage product framing, LLM honest qualifier ("own projects"), discovery signals, no BD/Sales framing. CSAT 73% added to PROFILE + both CVs. GitHub contact rendering fixed (inline with contacts, not standalone heading). CV cutoff rule: 2017 — documented in PROFILE.md, pre-2017 entries (General Servers, SBC Distribution, Sole Proprietor) removed from EN CV. Cover letter UA — iterated from formulaic to direct narrative style (final approved). `interview_prep.md` saved (LLM cost/quality trade-offs Q&A). Ukrainian CV pre-2017 cleanup pending. | ~2.5 h (estimated) |
| 01.06.2026 *(3rd session)* | 1 | **Housekeeping**: gitignore `skill/` directory (personal profiles + local pipeline artifacts). | ~15 min |
| 01.06.2026 *(4th session)* | 28 | **EPIC-13–18 + EPIC-17 Phase 1 + cleanup** (single git cluster 19:13–22:53). **EPIC-13** multi-user data model: `users` table, `user_id` FK, default user seeding, user-scoped vacancy paths, tracker filter, 241 tests. **EPIC-14** services/pdf: `render.py` + FastAPI `/render`, CVAdapter subprocess→httpx. **EPIC-15** services/parser: stripped knowledge-mirror-parser → own `jd-parser` service, djinni+dou only, docker-compose updated. **EPIC-18** rename `agent-hub` → `career-agent`. Multi-skill routing Phase 1: `prompts/pm/` + `prompts/generic/`, `skill_type` in AgentDeps. **EPIC-16** job-monitor: move + redesign, webhook push, file polling→DB polling. **ARCHITECTURE.md**: 5 pipeline modes, components, phase table. `/pipeline` slash command + `e2e_test.py --file/--id`. Pipeline user sync + `--auto-confirm` (relay pattern for non-TTY). `kmp-service` → `jd-parser` rename (13 files, `KMPAdapter→ParserAdapter`). **EPIC-17 Phase 1** onboarding: FSM (`awaiting_name→skill→pdf`), PDF upload (pypdf), `profile_json` + `onboarding_step` in DB, `/update_profile`, `/set_skill`, `ClaudeProvider` loads profile from DB (PROFILE_MD_PATH deprecated), `MULTI_USER_ENABLED` flag, `core/onboarding.py`, `tools/cv_onboard.py`, interview prompt stubs (DESIGN REQUIRED). kmp legacy purge (active code). `test_cv_adapter.py` (P1 closed). **253 tests total.** | ~3 h 40 min (19:13–22:53) |
| 02.06.2026 | — (no commits) | **Pipeline hardening + e2e verify** (279→291 tests). **Tracker UI verify** — confirmed #65–69 batch DOU vacancies parse correctly; BotsCrew rec fixed (`apply`→`take a chance`). **Batch inbox fix** — `scripts/vacancy_track.py`: new `delete-inbox` subcommand (path traversal guard, idempotent); `skill/SKILL.md` + `analyze.md`: `move-to-inbox` → `delete-inbox` in batch/sequential flows; stray DOIT Software duplicate folder removed. **e2e generate+cover #48** — full pipeline verified ($0.10, CV.md ✅ PDF ✅ Cover.md ✅); fixed `services/pdf/render.py` font path bug (relative `services/pdf/fonts/` → absolute `_PROJECT_ROOT/fonts`; added `load_dotenv`). **Inbox deduplication** — `SKILL.md` Sequential Mode step a.5 (URL grep → skip/reprocess); Batch Mode silent skip; `analyze.md` dedup note. **EPIC-20 Task #5** — `phase1_analysis.md` (pm+generic): added `## 1.0 Vacancy Header` output block; `cv_analyze.py`: `_extract_vacancy_title()` helper parses `Role — Company` from Phase 1 output → updates DB title; 8 new tests. **`health_check.py`** — `scripts/health_check.py`: HTTP checks (parser + pdf-service), SQLite, optional Telegram bot check + alert, exit 0/1. **`rec_class` fix** — `web/reader.py`: `take a chance` / `рассмотреть` checked before generic `подавать` (substring trap); 4 new tests. **CLAUDE.md v1.4** — services live, scripts expanded, fonts note, new commands. **Docs** — EPIC-14 font path corrected, EPIC-20 status→In Progress, session log created, multi-skill Phase 2 closed (already in EPIC-17 Phase 1). | ~4 h (estimated) |
| 04.06.2026 *(2nd session)* | 2 (career-agent) | **Pipeline hardening + PDF renderer fixes + Zeely CV**. **`scripts/inbox_scan.py`** — canonical recursive inbox_manual scanner (title + `Source:` URL parse, dedup vs `inbox/{user_id}/*/JD.md` → `seen`/`seen_path`, `raw_folder` for delete-inbox, `--json`). Root cause of recurring inbox-skip: non-recursive `ls` missed folder-based drops → false "empty". **`/analyze` Step 0 → combined two-block menu** (profile/mode + inbox in one message, vertical-split `│` columns, no round-trip); both `SKILL.md` + `analyze.md` rewired to the scanner + menu. **`services/pdf/render.py` cover-aware fix** — `render_md` forced first 3 lines into CV name/headline/contacts blocks (no wrap) → covers overflowed; now CV-header parsing only when a contacts-links line is present, else wrapped body. Regression-tested EN/UA CV + cover. **CV contacts standardized** — `prompts/pm/phase3_cv_draft.md` (gitignored) fixed line `Email · Telegram · LinkedIn · ioncat.github.io` (LinkedIn always, no GitHub); inconsistency had silently dropped LinkedIn from recent CVs. **Two-renderer cleanup** — `services/pdf/render.py` copied from deprecated `../callback-cv/cv_to_pdf.py` (identical buggy render_md); live `CVAdapter` already on service, but `SKILL.md` PDF section + `analyze.md -pdf` still pointed to `cv_to_pdf.py` → rewritten to service-only. External repo left untouched (not ours). **Zeely AI CV** (DB #80) — Phase 1+2 (fit 7/10, apply, Operations/BizOps) + Phase 3+3.5 Ukrainian CV (many user edits); vacancy closed by user, no cover. CLAUDE.md v1.5, BACKLOG.md updated, 4 memory files. ⚠️ Estimated — most work was iterative review/discussion, not commit-clustered. | ~3 h (estimated) |
| 14.06.2026 | — | **VScore + Recommendation Matrix**: `phase1_analysis.md` §1.7 (VScore 8-dim formula, domain_score), `phase2_fit.md` Fit×VScore matrix (hard blockers / take a chance / limited upside labels), `skill/SKILL.md` Quick Scan format updated, `web/reader.py` + `tracker.html` VScore column (green/amber/gray). `skill/users/1/PROFILE.md` Vacancy Preferences section. | ~3 h (estimated) |
| 15.06.2026 | 14 (career-agent) | **EPIC-21 Task 1 + architecture history + RSS automation + local startup.** EPIC-21 rev 2 rewrite: Phase 2.5 as interactive pause-state, VScore composite → Python (Task 3), weasyprint decided. UML FSM diagram (`docs/diagrams/EPIC-21-pipeline-fsm.html`). ARCHITECTURE.md: Phase history table + ADR-02 (deterministic FSM). BACKLOG: Post-EPIC-21 freshness review. **Task 1 (weasyprint)**: `services/pdf/render.py` full rewrite 520→50 lines (markdown→Jinja2→weasyprint), `cv.html` A4 template, Docker pango/cairo libs. per-user `skill/SKILL.md` (users/1/SKILL.md). **RSS automation**: `scripts/import_seen_jobs.py` (seen_jobs.json→DB, --today, --dry-run); 31 vacancies imported. `services/job-monitor/feeds.json` (7 feeds). `.env` path fix in monitor.py. docker-compose :ro fix. **Tracker**: per-user counts in date group headers (Alex: N · Maria: N). **Startup**: `run_*.bat` × 5 + `start.vbs` (Windows Terminal split-pane). DB: user_id telegram_chat_id mapping fixed (duplicate user_id=3 removed). | ~6 h (estimated) |
| 17.06.2026 | 9 (career-agent) | **RSS watcher hardening + inbox folder naming + Ollama provider.** 4 commit clusters (10:09–20:04). **Cluster 1**: `launcher.py` — per-service file logs (`logs/parser.log` etc.), filtered terminal output. **Cluster 2**: `rss_watcher.py` — notify-first on new vacancy (Telegram before processing), salary extraction from title, status machine fix; `prompts/pm/phase3_cv_draft.md` — markdown structure rule; `db/schema.sql` + `database.py` — `published_at` column (RSS pub date, backfilled). **Cluster 3**: `web/api.py` — `_site_from_url()`, site set at webhook insert (fixes "?" badge); `cv_fetch_jd.py` — folder named from parsed title (not URL slug); `rss_watcher.py` — concurrent processing via `asyncio.gather`. **Cluster 4**: `cv_fetch_jd.py` + `services/parser/app.py` + `contracts/parsed_document.py` — `{id} — {role} — {company}` folder format, DB insert before folder creation, `ParsedDocument.company`, `_extract_company()` (DOU URL slug / Djinni `<title>`), `_safe_folder_name()`. `core/llm_client.py` — `OllamaProvider` real httpx impl (POST `/api/chat`, 300s timeout, `LLMUnavailableError` on connect fail); `core/settings.py` + `agent.py` — `LLM_PROVIDER`/`OLLAMA_BASE_URL`/`OLLAMA_MODEL` env vars. 291→316 tests. | ~6 h (estimated) |
| 18.06.2026 | — (no commits) | **Ollama error handling + model testing.** `OLLAMA_TIMEOUT=0` → `read_timeout=None` in httpx (no-timeout mode for slow models); `done_reason` logging + `done_reason='length'` → `LLMError` (truncation detection). `scripts/test_ollama_pipeline.py` — `--phase 1\|2` flag (phase-by-phase manual testing). Tested 5 models on vacancy #120 (Product Owner iSpeedtoLead): qwen3:8b (19 min, ★★★), glm-5.2:cloud (403 paid, ❌), minimax-m3:cloud (truncates at ~1400 tokens silently, ❌), gemma4:31b-cloud (105s, full output, ★★★ — best for logic testing), cas/aya-expanse-8b (29s, poor quality, ★). Root cause of Ollama quality gap: all models miss cross-document reasoning (JD + PROFILE simultaneously) and PM archetype signals (NOT Founder Proxy, fast-hire culture). Strategy: Ollama for logic testing (free, iterative), Claude for final quality run. 316→320 tests (4 new error path tests). | ~3 h (estimated) |
| **Total** | | | **~50 h** |

*Table updated after each session.*
